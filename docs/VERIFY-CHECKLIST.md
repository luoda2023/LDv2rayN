# LDv2rayN 手动验证清单

> 用途：逐项检查 AI 相关 UI 功能是否正常，每项标记 ✅ PASS / ❌ FAIL（附现象描述）。
> 测试前请确认运行的是最新构建的 EXE：
> `D:\LUODA\LDv2rayN\v2rayN\bin\Release\net10.0-windows10.0.19041.0\win-x64\LDv2rayN.exe`

## 0. 准备

- [ ] 启动 EXE 后，主窗口正常打开，无报错弹窗
- [ ] 状态栏显示核心运行（如 `sing_box`），无"未找到 Core 文件"提示

---

## 1. 托盘图标

| # | 操作 | 预期结果 | 结果 |
|---|------|---------|------|
| 1.1 | 看右下角系统托盘（时钟旁边，必要时点"隐藏图标"展开） | 能看到 LDv2rayN 图标（标准 ICO，不是空白/方块） | ☐ |
| 1.2 | **左键单击**托盘图标 | 弹出 AI 对话框 + 主窗口恢复显示 | ☐ |
| 1.3 | **右键**托盘图标 | 弹出右键菜单（系统代理、路由、服务器、更新订阅、退出等） | ☐ |
| 1.4 | 右键菜单点「退出」 | 程序完全退出，托盘图标消失 | ☐ |

## 2. 主窗口右下角浮动按钮

| # | 操作 | 预期结果 | 结果 |
|---|------|---------|------|
| 2.1 | 主窗口右下角 | 能看到深灰（卡片色）圆角按钮，白色机器人图标 | ☐ |
| 2.2 | **单击**浮动按钮 | 弹出 AI 对话框（与托盘左键同一个实例，聊天记录保留） | ☐ |
| 2.3 | 再点一次浮动按钮 | AI 对话框隐藏；再点恢复 | ☐ |
| 2.4 | 悬浮在按钮上 | 提示"AI 智能代理助手" | ☐ |

## 3. AI 聊天面板（悬浮对话框）

| # | 操作 | 预期结果 | 结果 |
|---|------|---------|------|
| 3.1 | 打开 AI 对话框 | 位于主窗口右下角附近，无边框、置顶、不出现在任务栏 | ☐ |
| 3.2 | 标题栏 | 显示"AI 助手"，右侧三个图标按钮：⚙（配置）—（最小化）✕（关闭），**图标无乱码/方块** | ☐ |
| 3.3 | 标题栏**中文/emoji 显示** | "AI 助手"及欢迎消息中的 emoji（🤖🔗📋 等）正常渲染，无 � 替换符 | ☐ |
| 3.4 | 拖动标题栏 | 窗口可移动 | ☐ |
| 3.5 | 点 ⚙ 按钮 | 弹出 AI 设置窗口（API 地址/密钥/模型/分组/间隔可改） | ☐ |
| 3.6 | 点 — 按钮 | 对话框隐藏（程序不退） | ☐ |
| 3.7 | 关闭后再从托盘/按钮打开 | 秒开（已预热缓存），聊天记录还在 | ☐ |
| 3.8 | 输入框输入文字按回车 | 发送消息，显示处理中 | ☐ |

## 4. 气泡宽度（3/4 窗口宽）

| # | 操作 | 预期结果 | 结果 |
|---|------|---------|------|
| 4.1 | 窗口默认宽 380px 时发一条长消息 | 气泡最大宽度 ≈ 窗口宽度的 3/4（约 285px），不超出 | ☐ |
| 4.2 | 拖动窗口边缘拉宽 | 气泡最大宽度同步变宽（保持 3/4） | ☐ |
| 4.3 | 用户消息 | 右对齐、深色气泡；AI 消息左对齐；系统消息居中窄条 | ☐ |

## 5. 富文本渲染（Markdown）

| # | 输入 | 预期结果 | 结果 |
|---|------|---------|------|
| 5.1 | 发消息含 `**粗体**` | 显示为粗体文字（不是裸星号 `**粗体**`） | ☐ |
| 5.2 | 发消息含 `` `code` `` | 反引号内容以等宽字体+蓝字+深底显示 | ☐ |
| 5.3 | 多行文本（换行） | 正确换行，无多余空行 | ☐ |
| 5.4 | 发一个链接 `https://raw.githubusercontent.com/ripaojiedian/freenode/main/sub` | 显示"检测到/正在下载分析"，随后报告提取/添加结果（进度条出现） | ☐ |
| 5.5 | 搜索/分析过程中 | 输入框上方出现**进度条 + "正在搜索免费节点，请稍候…"**，完成后消失 | ☐ |
| 5.6 | 分析一个 Base64 订阅链接 | 能解码并添加节点（消息显示添加 N 个） | ☐ |

## 6. 顺手检查（相关功能）

| # | 操作 | 预期结果 | 结果 |
|---|------|---------|------|
| 6.1 | 菜单「🤖 AI智能获取」 | **总是弹出** AI 设置窗口（不是只弹消息框） | ☐ |
| 6.2 | 菜单「📋 查看 AI 日志」 | 弹出 AI 日志/状态窗口 | ☐ |
| 6.3 | 菜单「💬 AI代理助手」 | 弹出 AI 对话框 | ☐ |
| 6.4 | 状态栏测延迟（双击运行信息） | 显示明确结果：正常显示 `延迟 xx ms`，核心未运行显示"⚠️ 核心未运行"，节点不可达显示"❌ 节点不可达"（不再是裸 `-1 ms, none`） | ☐ |

---

## 结果汇总

- PASS 项数：____ / 26
- FAIL 项编号：________________
- 失败现象描述：________________

填好后把 FAIL 项编号和现象发给我，我针对性修复。